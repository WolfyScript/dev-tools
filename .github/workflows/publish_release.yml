name: publish_release.yml
on:
  workflow_dispatch: {}
  release:
    types:
      - "published"

jobs:
  publish-gradle:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout project sources
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin

      - name: Setup Gradle
        uses: gradle/action/setup-gradle@v4

      - name: Execute Gradle build
        run: gradle publish
        env:
          wolfyRepoPublishUsername: ${{ secrets.ARTIFACTORY_USERNAME }}
          wolfyRepoPublishToken: ${{ secrets.ARTIFACTORY_TOKEN }}

